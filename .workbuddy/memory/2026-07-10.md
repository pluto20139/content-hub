- 2026-07-10 用户反馈 3 个问题：(1) 手机点击卡片不调起 App（Deep Link 失效）；(2) 网站加载慢；(3) 要求 review code。
- 诊断结果：
  - Deep Link 根因：`ContentCard.tsx` 中 `navigator.clipboard.writeText()` 异步 Promise 打断了 iOS Safari 的用户手势链，导致 `window.location.href = deepLink` 被静默拦截。
  - 加载慢根因：无客户端缓存（每次刷新/切 Tab 都请求 Supabase）、图片无懒加载、无骨架屏、Tab 切换丢弃数据。
  - Code Review 发现：`013` 迁移用 `USING(true)` 放开 anon 读取所有 contents（含 30 天软删除）；modal 全局变量冲突；ContentCard 无可访问性；无 Error Boundary；react-router-dom 死依赖；已隐藏 Tab 隐藏按钮逻辑 Bug。
- 已输出 `V1.2_TASK_PLAN.md`，包含 12 个 Task（2 个 P0 + 2 个 P1 + 5 个 P2 + 3 个 P3），并在任务系统创建 #46-#50 追踪。
- 需用户确认：(1) 已隐藏 Tab 是否对匿名开放；(2) 是否支持取消隐藏；(3) 是否需要 PWA。
- 用户确认进入 V1.2 迭代：接入 Dify 工作流生成 AI 内容总结 + 新增详情页 + 卡片点击改为进详情页 + 详情页底部常驻"跳转到 App"按钮。
- 用户确认全部 17 项决策：无 Dify 实例（等去弄）、让我教配 Dify、全部内容都总结、Dify 内部处理各平台差异、不需要分享/隐藏按钮、骨架屏+轮询、失败+重试、summary 不限长度、加 summary_duration_ms、超时 30s、重试 1 次间隔 5s、并发 ≤5 但不遗漏、支持轮询、回 Feed 原位置、Admin 无需手动触发和查看。
- 已输出 `PRD_V1.2.md`（正式 PRD）、`DIFY_WORKFLOW_GUIDE.md`（Dify 配置指南）、`V1.2_TASK_PLAN.md`（14 个 Task 的完整计划，约 20h，覆盖 V1.2 + V1.1 遗留修复）。
- 用户已自行实现 AI 总结功能（卡片内折叠展开方式，非详情页），并完成苹果系极简留白 UI 改造。Review 发现 4 个 P2（no-scrollbar 未定义、占位图尺寸旧、FallbackModal 旧配色、Feed 底部状态旧色）+ 3 个 P3，已全部修复。
- UI 设计风格确认：苹果系极简留白、浅色模式、不做暗色、卡片内折叠展开（不做详情页）、平台标签浅色底+品牌色文字、AI 总结用中性灰底非紫色。

