- 2026-07-04 进入 V1.1 迭代需求讨论，用户确认关键决策：不新增独立隐藏字段（复用 is_display）、隐藏功能面向匿名用户且不做防滥用、抖音必须上（采用 RSSHub + 代理池 + 自托管）、不允许生产环境 Mock 数据、monitors 新增 native_type 字段。
- 已输出整理后的 PRD 文件 `PRD_V1.1.md` 和具体改动清单 `V1.1_CHANGE_LIST.md`。
- 已输出 V1.1 任务计划 `V1.1_TASK_PLAN.md`，并在任务系统中创建 12 个追踪任务（任务 18-25），设置了依赖关系。
- 已完成对 `V1.1_TASK_PLAN.md` 的 Review，输出 `V1.1_TASK_PLAN_REVIEW.md`，识别出 P0/P1/P2 级遗漏与状态冲突。
- 已修复 `V1.1_TASK_PLAN.md`：新增前置调研任务 0；任务 1 补充 native_type CHECK/索引/RLS 测试；任务 2 解决知乎 post Deep Link 需要 monitor_native_id；任务 3 明确短链缓存与代理；任务 6 拆分 ProxyPool 接口与反爬状态；任务 8 加隐藏失败回滚；任务 9 image-proxy 改为必选兜底；任务 11 合并 .env.example 更新；任务 12 加 RLS/短路/隐藏-软删除交互测试。
- 已完成对修复后 `V1.1_TASK_PLAN.md` 的二次 Review，输出 `V1.1_TASK_PLAN_REVIEW_V2.md`，剩余 1 个 P0（short_link_cache 表缺失）、5 个 P1（代理脱敏、知乎 post fallback、新平台状态流转、小红书移动域名、抖音图集映射）、3 个 P2 细节待补。
- 已二次修复 `V1.1_TASK_PLAN.md` 并同步调整 `PRD_V1.1.md`：新增 short_link_cache 表与 RLS、扩展 platform_configs_admin 视图脱敏、明确新平台状态机、抖音图集映射为 video、小红书兜底 URL 以 task plan 为准（PRD 同步为 www.xiaohongshu.com）、明确 image-proxy 接口与知乎 post fallback。
- 已完成 V1.1 修复后的二次代码 Review，输出 `CODE_REVIEW_V1_1_V2.md`：上一轮 P0 全部关闭，剩余 3 个 P1（知乎 article/answer Deep Link schema、YouTube /c/ 未支持、B站/YouTube native_type 与 PRD 不符），7 个 P2（30 天软删除对 anon 可见需确认、H5 占位图未用品牌色、short-link.ts 死代码、知乎专栏路由名不一致、RSSHub 超时缺失、Admin 未展示平台暂停状态、config.toml 未使用 secret）。
- 已修复添加博主问题：修改 `supabase/functions/parse-url/index.ts` 支持抖音短链 `iesdouyin.com/share/user/?sec_uid=...`；增强小红书短链解析，HEAD 失败后降级 GET 并支持页面内容提取；优化 Admin 输入框占位文案。输出修复记录 `BUGFIX_ADD_MONITOR.md`。知乎状态异常需进一步确认 RSSHub 配置和链接。
