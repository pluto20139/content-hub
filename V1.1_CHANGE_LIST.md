# V1.1 改动清单

> 按模块列出的具体文件、代码改动点与风险等级。实现时请按此清单逐项核对。

---

## 1. 数据库迁移（`supabase/migrations/`）

| 改动 | 文件/位置 | 风险等级 | 说明 |
|---|---|---|---|
| 新增迁移 `012_v1_1_platforms_and_native_type.sql` | `supabase/migrations/012_v1_1_platforms_and_native_type.sql` | **P0** | 扩展 platform 枚举，新增 `native_type` 字段，新增 anon 隐藏策略 |
| 扩展 `monitors.platform` CHECK | 同上 | **P0** | 从 `('bilibili','youtube','zhihu')` 改为 `('bilibili','youtube','zhihu','douyin','xiaohongshu')` |
| 扩展 `contents.platform` CHECK | 同上 | **P0** | 同上 |
| 扩展 `platform_configs.platform` CHECK | 同上 | **P0** | 同上；`platform_configs` 表新增 douyin/xiaohongshu 相关配置行 |
| `monitors` 新增 `native_type VARCHAR(20)` | 同上 | **P0** | 非空仅在 `zhihu` 时强制，其余平台可空；添加 `idx_monitors_native_type` 索引 |
| 新增 `contents_anon_hide` RLS 策略 | 同上 | **P0** | 允许 anon UPDATE 只改 `is_display` 为 `false` |
| 更新 `contents` 表注释 | 同上 | **P1** | 注释 `is_display` 同时表示“用户隐藏”和“30 天软删除” |

---

## 2. Edge Functions（`supabase/functions/`）

| 改动 | 文件/位置 | 风险等级 | 说明 |
|---|---|---|---|
| 扩展 `parse-url` 平台识别 | `supabase/functions/parse-url/index.ts` | **P0** | 新增知乎、抖音、小红书分支；返回 `native_type` |
| 知乎解析 | 同上 | **P1** | 正则匹配 `zhihu.com/people/:id`（native_type=people）和 `zhuanlan.zhihu.com/:id`（native_type=column） |
| 抖音短链解析 | 同上 | **P1** | 通过代理 + HEAD 请求跟踪 `v.douyin.com` 重定向，提取 `sec_uid` |
| 小红书短链解析 | 同上 | **P1** | 通过代理 + HEAD 请求跟踪 `xhslink.com` 重定向，提取 `uid` |
| 昵称获取 | 同上 | **P2** | 知乎 RSSHub、抖音/小红书 HTML 或 RSSHub 获取昵称；失败时降级为默认格式 |
| 新增 `image-proxy` Edge Function | `supabase/functions/image-proxy/index.ts` | **P2** | 服务端转发图片流，作为 B站封面 403 的最终兜底 |
| `bilibili-auth` | 无改动 | - | V1.1 不涉及 |

---

## 3. Cron 抓取引擎（`scripts/cron/src/`）

| 改动 | 文件/位置 | 风险等级 | 说明 |
|---|---|---|---|
| 扩展 `PlatformAdapter` 接口 | `scripts/cron/src/adapters/types.ts` | **P0** | `platform` 类型增加 `'douyin' \| 'xiaohongshu'`；接口增加可选 `native_type` 读取 |
| 新增 `ZhihuAdapter` | `scripts/cron/src/adapters/zhihu.ts` | **P0** | 基于 RSSHub 获取个人主页/专栏内容；按 `native_type` 路由 |
| 新增 `DouyinAdapter` | `scripts/cron/src/adapters/douyin.ts` | **P0** | 自托管 RSSHub + 代理池 + 移动网页兜底；含反爬感知和平台级短路 |
| 新增 `XiaohongshuAdapter` | `scripts/cron/src/adapters/xiaohongshu.ts` | **P0** | 自托管 RSSHub + 移动网页兜底 |
| 新增代理池模块 | `scripts/cron/src/lib/proxy.ts` | **P1** | 管理代理列表、失败计数、健康检查、随机选择 |
| 新增短链解析模块 | `scripts/cron/src/lib/short-link.ts` | **P1** | 解析抖音/小红书短链，缓存 `short_code -> native_id` |
| 修改 `scheduler.ts` / `index.ts` | `scripts/cron/src/scheduler.ts` | **P1** | 注册 5 个平台适配器，知乎/抖音/小红书同平台限速 ≥1.5s |
| 修改 `content-writer.ts` | `scripts/cron/src/lib/content-writer.ts` | **P0** | 确保 UPSERT 不更新 `is_display` 和 `created_at`（防复活） |
| 修改 `alert.ts` | 同上 | **P2** | 新增知乎/抖音/小红书告警文案 |
| 修改 `throttle.ts` | `scripts/cron/src/lib/throttle.ts` | **P2** | 新平台纳入同平台限速调度 |
| 修改 `monitors-query.ts` | `scripts/cron/src/lib/monitors-query.ts` | **P2** | 查询 `native_type` 字段并透传给适配器 |

---

## 4. 共享包（`packages/shared/src/`）

| 改动 | 文件/位置 | 风险等级 | 说明 |
|---|---|---|---|
| 扩展平台常量 | `packages/shared/src/constants/platforms.ts` | **P0** | 新增 `zhihu`、`douyin`、`xiaohongshu` 中文名与品牌色 |
| 扩展 Deep Link 映射 | `packages/shared/src/constants/deep-link.ts` | **P0** | 新增知乎 4 种、抖音 1 种、小红书 1 种 scheme；无法匹配走 original_url |
| 环境检测 | `packages/shared/src/utils/environment.ts` | **P2** | 无改动，复用现有 UA 检测 |
| 时间格式化 | `packages/shared/src/utils/time.ts` | **P3** | 无改动，复用现有相对时间 |

---

## 5. H5 用户端（`apps/h5/src/`）

| 改动 | 文件/位置 | 风险等级 | 说明 |
|---|---|---|---|
| 扩展 Tab 配置 | `apps/h5/src/constants/tabs.ts` | **P0** | 新增知乎、抖音、小红书、已隐藏 4 个 Tab |
| 修改 `Feed.tsx` | `apps/h5/src/pages/Feed.tsx` | **P0** | 支持 7 个 Tab 切换；已隐藏 Tab 查询 `is_display=false` |
| 修改 `ContentCard.tsx` | `apps/h5/src/components/ContentCard.tsx` | **P0** | 全局或图片级 `referrerPolicy="no-referrer"`；右上角新增隐藏按钮；点击阻止冒泡 |
| 新增隐藏按钮组件 | `apps/h5/src/components/HideButton.tsx` | **P2** | 32×32px 点击区，调用 PATCH 更新 `is_display=false` |
| 修改 Supabase 客户端 | `apps/h5/src/lib/supabase.ts` | **P2** | 确保 anon 角色可发起 UPDATE（依赖 RLS 策略） |
| 空状态 | `apps/h5/src/pages/Feed.tsx` | **P2** | 已隐藏 Tab 为空时显示“暂无隐藏内容” |

---

## 6. 管理端 Admin（`apps/admin/src/`）

| 改动 | 文件/位置 | 风险等级 | 说明 |
|---|---|---|---|
| 扩展平台筛选下拉 | `apps/admin/src/pages/MonitorList.tsx` | **P1** | 平台筛选新增知乎、抖音、小红书 |
| 扩展平台颜色展示 | 同上 | **P2** | 新增 3 个平台 Tag 颜色 |
| 修改 `parse-url` 调用 | 添加监控逻辑 | **P1** | 接收并保存 `native_type` |
| 无新增隐藏管理功能 | - | - | 按 V1.1 决策，隐藏功能只在 H5 由用户操作 |

---

## 7. 配置与环境变量

| 改动 | 文件/位置 | 风险等级 | 说明 |
|---|---|---|---|
| 新增环境变量 | `.env.example` | **P0** | `RSSHUB_URL`、`DOUYIN_PROXY_LIST`、`XIAOHONGSHU_PROXY_LIST`、`ZHIHU_PROXY_LIST`、`IMAGE_PROXY_ENABLED` |
| 新增 `platform_configs` 预设行 | `supabase/seed.sql` | **P1** | 抖音/小红书 RSSHub 配置、代理状态标记 |
| 更新部署文档 | `腾讯云部署执行计划.md` / `PROJECT_CONTEXT.md` | **P2** | 补充 RSSHub 自托管、代理池配置 |

---

## 8. 测试与验收

| 改动 | 文件/位置 | 风险等级 | 说明 |
|---|---|---|---|
| 新增适配器单元测试 | `scripts/cron/src/adapters/__tests__/` | **P1** | 重点测短链解析、内容标准化、平台级短路判定 |
| 新增 `parse-url` 测试 | `supabase/functions/parse-url/` | **P2** | 覆盖 5 个平台 URL 用例 |
| 手动真机测试 | H5 | **P1** | 验证 Deep Link 在小红书/抖音/知乎的唤醒成功率 |

---

## 9. 风险等级汇总

| 风险等级 | 涉及项 |
|---|---|
| **P0（必须实现且无退路）** | 数据库枚举扩展、`native_type` 字段、新增 3 个适配器、RLS 隐藏策略、H5 Tab 与隐藏按钮、Deep Link 扩展、封面 referrerPolicy |
| **P1（高优先级，但可有限降级）** | 抖音代理池/反爬、短链解析、RSSHub 自托管、昵称获取降级、管理端平台筛选 |
| **P2（体验增强或兜底）** | `image-proxy` 兜底、单元测试、已隐藏空状态、Deep Link 真机实测 |
| **P3（文档/运维）** | `.env.example` 更新、部署文档更新 |
